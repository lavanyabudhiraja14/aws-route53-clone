import { ComingSoon } from "@/components/ComingSoon";

export default function HealthChecksPage() {
  return (
    <ComingSoon
      title="Health checks"
      description="Create health checks to monitor endpoints and fail over DNS when resources become unhealthy."
    />
  );
}
